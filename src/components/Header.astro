---
import { loadQuery } from "../../sanity/lib/load-query";
import { headerQuery } from "../../sanity/lib/queries";
import HeaderComponent from "./HeaderComponent";
import type HeaderProps from "./HeaderComponent";

const { data: header } = await loadQuery<typeof HeaderProps>({
  query: headerQuery,
});
---

<HeaderComponent header={header} client:load />
