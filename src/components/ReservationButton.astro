---
// ReservationButton.astro
import Button from "./Button.astro";

export interface ReservationButtonProps {
  variant?: "primary" | "secondary" | "outline" | "danger";
  size?: "sm" | "md" | "lg";
  url: string;
  disabled?: boolean;
}

const props = Astro.props as ReservationButtonProps;
---

<Button {...props}>
  <slot />
</Button>
