---
import { fetchIssuedTickets } from "../api/tickets";
import { loadQuery } from "../../sanity/lib/load-query";
import { homePageQuery } from "../../sanity/lib/queries";
import Layout from "../layouts/Layout.astro";
import ReservationButton from "../components/ReservationButton.astro";

const issuedTickets = await fetchIssuedTickets();

const numberOfTickets = issuedTickets.data.length;
const TOTAL_SEATS = 20;

interface HomePageQuery {
  highlightEvent: {
    title: string;
    eventDate: string;
    location: string;
    image?: string;
    ticketUrl: string;
  };
  planForVisit: {
    title: string;
    editor: string; // Assuming editor is a string (update type if needed)
    description: string;
    images: {
      image: string; // URL of the image
    }[];
  };
  linkToGallery: {
    title: string;
    image: string;
    slug: string;
    description: string;
    pictures: {
      url: string;
      altText: string;
    }[];
  };
}

const { data: homePage } = (await loadQuery({ query: homePageQuery })) as {
  data: HomePageQuery;
};
---

<Layout>
  <!-- Highlight Event -->
  <section>
    <div
      class="flex flex-col px-8 pt-10 md:flex-row lg:h-screen lg:pt-0 justify-center items-center"
    >
      <!-- Image -->
      <div
        class="h-80 w-80 md:h-96 md:w-120 relative isolate overflow-hidden rounded-2xl bg-gray-900 px-8 pb-8 pt-80 sm:pt-48 lg:pt-80 lg:h-128 group"
      >
        <!-- Animated Frame -->
        <div
          class="absolute inset-0 z-10 rounded-2xl border-8 border-transparent animate-glow"
        >
        </div>

        {
          homePage.highlightEvent.image && (
            <img
              src={homePage.highlightEvent.image}
              alt={homePage.highlightEvent.title}
              class="absolute inset-0 -z-10 size-full object-cover"
            />
          )
        }
      </div>
      <!-- Event Details -->
      <div
        class="w-full md:w-128 flex flex-col items-center pt-10 md:items-start md:px-8 lg:ml-20"
      >
        <h2>
          {homePage.highlightEvent.title}
        </h2>
        <p class="text-lg lg:text-xl mt-4">
          Date: {
            new Date(homePage.highlightEvent.eventDate).toLocaleDateString()
          }
        </p>
        <p class="text-lg lg:text-xl mt-2">
          Location: {homePage.highlightEvent.location}
        </p>
        {
          homePage.highlightEvent.ticketUrl &&
            (numberOfTickets < TOTAL_SEATS ? (
              <ReservationButton
                url={homePage.highlightEvent.ticketUrl}
                size="lg"
              />
            ) : (
              <p class="mt-4 px-4 py-2 bg-amber-200">
                Table reservation is closed for this event. Tables are available
                first come first serve upon arrival.
              </p>
            ))
        }
      </div>
    </div>

    <!-- Plan for your visit Section -->
    <section>
      <div class="w-full p-10 lg:items-center">
        <div class="mx-auto max-w-2xl text-center">
          <h2>
            {homePage.planForVisit.title}
          </h2>
          <p class="mt-2 text-lg/8">
            {homePage.planForVisit.description}
          </p>
        </div>
        <div
          class="mx-auto mt-16 grid max-w-2xl auto-rows-fr grid-cols-1 gap-8 sm:mt-20 lg:max-w-256 lg:h-128"
        >
          <article
            class="relative isolate flex flex-col justify-end overflow-hidden rounded-2xl bg-gray-900 px-8 pb-8 pt-80 sm:pt-48 lg:pt-80"
          >
            <img
              alt=""
              src={homePage.planForVisit.images[0].image}
              class="absolute inset-0 -z-10 size-full object-cover"
            />
            <h3>
              <a href={homePage.planForVisit.slug} class="group">
                <span class="absolute inset-0"></span>
              </a>
            </h3>
          </article>
        </div>
      </div>
    </section>

    <!-- Link To Gallery Section -->
    <section>
      <div class="w-full p-10 lg:items-center lg:h-screen">
        <div class="mx-auto max-w-2xl text-center">
          <h2>
            {homePage.linkToGallery.title}
          </h2>
          <p class="mt-2 text-lg/8">
            {homePage.linkToGallery.description}
          </p>
        </div>
        <div
          class="mx-auto mt-16 grid max-w-2xl auto-rows-fr grid-cols-1 gap-8 sm:mt-20 lg:max-w-256 lg:h-128"
        >
          <article
            class="relative isolate overflow-hidden rounded-2xl bg-gray-900 px-8 pb-8 pt-80 sm:pt-48 lg:pt-80"
          >
            <img
              alt={homePage.linkToGallery.pictures[0].altText}
              src={homePage.linkToGallery.pictures[0].url}
              class="absolute inset-0 -z-10 size-full object-cover"
            />
            <h3>
              <a href={homePage.linkToGallery.slug} class="group">
                <span class="absolute inset-0"></span>
              </a>
            </h3>
          </article>
        </div>
      </div>
    </section>
  </section>
</Layout>
